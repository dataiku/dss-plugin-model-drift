{
    "meta": {
        "label": "There is no model available, we compare two datasets with no target variable",
        "description": "Compare a potential drift between two datasets.",
        "icon": "icon-puzzle-piece"
    },

    "kind": "PYTHON",
    
    "inputRoles" : [
        {
            "name": "original",
            "label": "Original dataset",
            "description": "The dataset where the model was trained.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        },
        {
            "name": "new",
            "label": "New dataset",
            "description": "The new dataset.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Output dataset",
            "description": "The dataset containing the correlations.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [],

    "resourceKeys": []

}
