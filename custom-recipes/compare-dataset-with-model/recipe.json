{
    "meta": {
        "label": "Compute feature drift of a deployed model",
        "description": "Measure a potential drift between the original training data of a model and a new dataset. Support regression and classification",
        "icon": "icon-anchor",
        "displayOrderRank": 1
    },

    "selectableFromDataset": "input",

    "kind": "PYTHON",

    "paramsPythonSetup": "compute_model_id_choice.py",
    
    "inputRoles" : [
        {
            "name": "model",
            "label": "Deployed model",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsSavedModel": true
        },
        {
            "name": "input",
            "label": "New dataset",
            "description": "New dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Metrics dataset",
            "description": "Dataset storing drift metrics",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "use_active_version",
            "label": "Use the model's active version",
            "type": "BOOLEAN",
            "defaultValue": true
        },
        {
            "name": "version_id",
            "label": "Model version",
            "type": "SELECT",
            "getChoicesFromPython": true,
            "visibilityCondition": "!model.use_active_version"

        },
        {
            "name": "metric_list",
            "label": "Metrics",
            "type": "MULTISELECT",
            "selectChoices": [
                { "value": "drift_score", "label": "Drift score"},
                { "value": "fugacity", "label": "Fugacity"},
                { "value": "riskiest_features", "label": "Riskiest features"},
                { "value": "feature_importance", "label": "Feature importance"}
            ],
            "defaultValue": ["drift_score"]
        }
    ],

    "resourceKeys": []

}
