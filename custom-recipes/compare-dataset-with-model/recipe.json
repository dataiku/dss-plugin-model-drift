{
    "meta": {
        "label": "Compute data drift of a model deployed on the flow",
        "description": "Measure a potential data drift between the original training data of a model and a new dataset. Support regression and classification",
        "icon": "icon-puzzle-piece",
        "displayOrderRank": 1
    },

    "selectableFromDataset": "input",

    "kind": "PYTHON",

    "paramsPythonSetup": "compute_model_id_choice.py",
    
    "inputRoles" : [
        {
            "name": "model",
            "label": "Deployed model",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsSavedModel": true
        },
        {
            "name": "input",
            "label": "New dataset",
            "description": "New dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Metrics dataset",
            "description": "Dataset storing drift metrics",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "version_id",
            "label": "Model version",
            "type": "SELECT",
            "getChoicesFromPython": true
        }
    ],

    "resourceKeys": []

}
