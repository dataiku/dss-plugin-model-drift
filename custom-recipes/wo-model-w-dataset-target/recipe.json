{
    "meta": {
        "label": "There is no model available but there are two datasets and the target variable.",
        "description": "Measure a potential drift when having two datasets and a target variable",
        "icon": "icon-puzzle-piece"
    },

    "selectableFromDataset": "original",

    "kind": "PYTHON",
    
    "inputRoles" : [
        {
            "name": "original",
            "label": "Original dataset",
            "description": "Reference dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        },
        {
            "name": "new",
            "label": "New dataset",
            "description": "New dataset to compare",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Output dataset",
            "description": "The dataset containing the drift score and metrics",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "target_variable",
            "label" : "The target variable",
            "type": "COLUMN",
            "description": "Target variable (As a column in the reference dataset)",
            "mandatory" : true,
            "columnRole": "original"
        }
    ],

    "resourceKeys": []

}
