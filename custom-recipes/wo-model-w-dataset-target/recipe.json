{
    "meta": {
        "label": "There is no model available but there are two datasets and the target variable.",
        "description": "Get insights of a potential drift when having two datasets and a target variable",
        "icon": "icon-puzzle-piece"
    },

    "selectableFromDataset": "original",

    "kind": "PYTHON",
    
    "inputRoles" : [
        {
            "name": "original",
            "label": "Original dataset",
            "description": "The dataset where the model was trained.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        },
        {
            "name": "new",
            "label": "New dataset",
            "description": "The new dataset.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Output dataset",
            "description": "The dataset containing the correlations.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "target_variable",
            "label" : "The target variable",
            "type": "COLUMN",
            "description": "The name of the column in the dataset that is the target variable.",
            "mandatory" : true,
            "columnRole": "original"
        }
    ],

    "resourceKeys": []

}
