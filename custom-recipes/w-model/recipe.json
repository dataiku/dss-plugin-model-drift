{
    "meta": {
        "label": "The model is available.",
        "description": "Evaluate a drift between two datasets when a model is trained.",
        "icon": "icon-puzzle-piece"
    },

    "kind": "PYTHON",
    
    "inputRoles" : [
        {
            "name": "input",
            "label": "Input dataset",
            "description": "The dataset containing the raw data from which we'll compute correlations.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        },
        {
            "name": "model",
            "label": "Input model",
            "description": "Input model of predictions.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsSavedModel": true,
            "visibilityCondition": "model.advanced_activated"
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Output dataset",
            "description": "The dataset containing the correlations.",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "learning_task",
            "label": "Type of learning task",
            "type": "SELECT",
            "selectChoices": [
                {
                    "value": "REGRESSION",
                    "label": "A regression task"
                },
                {
                    "value": "CLASSIFICATION",
                    "label": "A classification task"
                }
            ],
            "description": "The type of learning task.",
            "mandatory": true
        }
    ],

    "resourceKeys": []

}
