{
    "meta": {
        "label": "There is no model available but there are two datasets and the target variable.",
        "description": "Measure a potential drift when having two datasets and a target variable",
        "icon": "icon-puzzle-piece"
    },

    "selectableFromDataset": "original",

    "kind": "PYTHON",
    
    "inputRoles" : [
        {
            "name": "original",
            "label": "Original dataset",
            "description": "Reference dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        },
        {
            "name": "new",
            "label": "New dataset",
            "description": "New dataset to compare",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "main_output",
            "label": "Output dataset",
            "description": "The dataset containing drift metrics",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {
            "name": "learning_task",
            "label": "Type of learning task",
            "type": "SELECT",
            "selectChoices": [
                {
                    "value": "CLASSIFICATION",
                    "label": "Classification"
                },
                {
                    "value": "REGRESSION",
                    "label": "Regression"
                }
            ],
            "mandatory": true
        },
        {
            "name": "target_variable",
            "label" : "Target variable",
            "type": "COLUMN",
            "mandatory" : true,
            "columnRole": "original"
        }
    ],

    "resourceKeys": []

}
